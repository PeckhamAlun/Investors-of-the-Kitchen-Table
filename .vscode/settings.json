{
  // Auto-save edits to disk shortly after you stop typing, and whenever the
  // editor loses focus (switching files/windows). No more lost unsaved buffers.
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
