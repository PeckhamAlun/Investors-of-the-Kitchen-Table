fastapi
uvicorn
python-dotenv
httpx
python-dateutil

# --- Document upload (POST /company/{ticker}/upload-document) ---
python-multipart      # FastAPI multipart/form-data parsing (file uploads)
python-docx           # .docx text extraction
pypdf                 # .pdf text extraction
langchain-text-splitters  # RecursiveCharacterTextSplitter chunking
google-genai          # Gemini embeddings for uploaded-doc chunks
pymongo               # company_financials storage (Atlas)

# --- PDF generation ---
weasyprint            # HTML/CSS -> PDF rendering (needs pango/cairo system libs — see nixpacks.toml)
reportlab             # PDF debate transcript generation

# --- Debate engine (root main.py, loaded at runtime via _load_debate_engine) ---
anthropic             # Claude API — agents' LLM
langgraph             # multi-agent debate orchestration (pulls in langchain-core)
requests              # scripts/analyse_company.py — FMP financial-data calls (auto-ingest)
