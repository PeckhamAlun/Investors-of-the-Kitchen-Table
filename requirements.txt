# Kitchen Table — Streamlit Community Cloud dependencies
# Python 3.11
# Only the packages needed to run app.py (debate engine + UI).
# Ingestion-only tools (yt-dlp, whisper, pdfplumber, etc.) are excluded.

# ── Web UI ──
streamlit>=1.30.0

# ── Debate engine (main.py imports) ──
anthropic
google-genai
langgraph
langchain-core

# ── Database ──
pymongo[srv]

# ── Company / market data ──
yfinance
requests
numpy
python-dateutil

# ── PDF transcript output ──
reportlab

# ── Config / secrets ──
python-dotenv
